@model IEnumerable<MVCASPCore.Models.Admin>
@*@using Microsoft.AspNetCore.Http;
    @if (Context.Session.GetInt32("AId") != null)
    {

    }
    else
    {

    }*@

@{
    ViewData["Title"] = "Index";
}
@using Microsoft.AspNetCore.Http;
@if (Context.Session.GetInt32("AId") != null)
{

    <h2>Admin</h2>

    <p>
        <a asp-action="Create">Create New</a>
    </p>
}
else
{

}
